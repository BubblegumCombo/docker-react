language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t username/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bmadjidi/docker-react npm run test
